import { HideFacilityLinksPipe } from './hide-facility-links.pipe';

describe('HideFacilityLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new HideFacilityLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
