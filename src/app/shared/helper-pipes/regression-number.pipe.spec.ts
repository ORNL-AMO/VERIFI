import { RegressionNumberPipe } from './regression-number.pipe';

describe('RegressionNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new RegressionNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
