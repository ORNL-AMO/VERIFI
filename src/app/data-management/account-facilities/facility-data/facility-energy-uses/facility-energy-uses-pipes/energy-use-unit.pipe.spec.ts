import { EnergyUseUnitPipe } from './energy-use-unit.pipe';

describe('EnergyUseUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyUseUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
