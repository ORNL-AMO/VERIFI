import { InvalidAnalysisPipe } from './invalid-analysis.pipe';

describe('InvalidAnalysisPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidAnalysisPipe();
    expect(pipe).toBeTruthy();
  });
});
