import { FacilityNamePipe } from './facility-name.pipe';

describe('FacilityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
