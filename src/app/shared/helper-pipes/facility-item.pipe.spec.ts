import { FacilityItemPipe } from './facility-item.pipe';

describe('FacilityItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityItemPipe();
    expect(pipe).toBeTruthy();
  });
});
