import { ReportBadgeClassPipe } from './report-badge-class.pipe';

describe('ReportBadgeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportBadgeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
