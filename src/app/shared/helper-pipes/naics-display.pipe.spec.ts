import { NaicsDisplayPipe } from './naics-display.pipe';

describe('NaicsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new NaicsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
