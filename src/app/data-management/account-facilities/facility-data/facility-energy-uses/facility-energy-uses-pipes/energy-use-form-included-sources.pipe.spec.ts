import { EnergyUseFormIncludedSourcesPipe } from './energy-use-form-included-sources.pipe';

describe('EnergyUseFormIncludedSourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyUseFormIncludedSourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
