# Unified UI/UX Migration Guide

This branch keeps the canonical migration guide at [`../unified-ux-migration.md`](../unified-ux-migration.md).

Use that guide for v0/v1 boundaries, `/p1` prototype guidance, `/v1` production routing, protected shared contracts, and just-in-time workflow decision notes.
