import { EnergyUseEquipmentInvalidPipe } from './energy-use-equipment-invalid.pipe';

describe('EnergyUseEquipmentInvalidPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyUseEquipmentInvalidPipe();
    expect(pipe).toBeTruthy();
  });
});
