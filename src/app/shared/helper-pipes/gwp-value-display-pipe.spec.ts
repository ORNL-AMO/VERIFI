import { GwpValueDisplayPipe } from './gwp-value-display-pipe';

describe('GwpValueDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new GwpValueDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
