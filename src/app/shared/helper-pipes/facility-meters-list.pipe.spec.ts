import { FacilityMetersListPipe } from './facility-meters-list.pipe';

describe('FacilityMetersListPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityMetersListPipe();
    expect(pipe).toBeTruthy();
  });
});
