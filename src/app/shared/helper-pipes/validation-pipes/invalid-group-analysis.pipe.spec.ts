import { InvalidGroupAnalysisPipe } from './invalid-group-analysis.pipe';

describe('InvalidGroupAnalysisPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidGroupAnalysisPipe();
    expect(pipe).toBeTruthy();
  });
});
