import { MeterDataQualityStatusPipe } from './meter-data-quality-status.pipe';

describe('MeterDataQualityStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterDataQualityStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
