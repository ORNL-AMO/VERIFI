import { AccountReportNamePipe } from './account-report-name-pipe';

describe('AccountReportNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountReportNamePipe();
    expect(pipe).toBeTruthy();
  });
});
