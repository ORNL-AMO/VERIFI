import { PredictorsOrderByPipe } from './predictors-order-by.pipe';

describe('PredictorsOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new PredictorsOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
