import { EnergyUseSourceFormGroupPipe } from './energy-use-source-form-group.pipe';

describe('EnergyUseSourceFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyUseSourceFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
