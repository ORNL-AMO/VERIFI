import { InvalidAccountAnalysisPipe } from './invalid-account-analysis.pipe';

describe('InvalidAccountAnalysisPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidAccountAnalysisPipe();
    expect(pipe).toBeTruthy();
  });
});
