import { AnalysisItemPipe } from './analysis-item.pipe';

describe('AnalysisItemPipe', () => {
  it('create an instance', () => {
    const pipe = new AnalysisItemPipe();
    expect(pipe).toBeTruthy();
  });
});
