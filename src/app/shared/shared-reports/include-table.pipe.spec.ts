import { IncludeTablePipe } from './include-table.pipe';

describe('IncludeTablePipe', () => {
  it('create an instance', () => {
    const pipe = new IncludeTablePipe();
    expect(pipe).toBeTruthy();
  });
});
