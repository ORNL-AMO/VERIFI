import { CharactersRemainingPipe } from './characters-remaining.pipe';

describe('CharactersRemainingPipe', () => {
  it('create an instance', () => {
    const pipe = new CharactersRemainingPipe();
    expect(pipe).toBeTruthy();
  });
});
