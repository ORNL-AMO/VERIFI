import { UserDefineModelDateRangePipe } from './user-define-model-date-range.pipe';

describe('UserDefineModelDateRangePipe', () => {
  it('create an instance', () => {
    const pipe = new UserDefineModelDateRangePipe();
    expect(pipe).toBeTruthy();
  });
});
