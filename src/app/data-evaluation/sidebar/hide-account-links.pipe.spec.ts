import { HideAccountLinksPipe } from './hide-account-links.pipe';

describe('HideAccountLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new HideAccountLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
