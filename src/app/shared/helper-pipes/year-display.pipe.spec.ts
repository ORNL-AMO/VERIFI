import { YearDisplayPipe } from './year-display.pipe';

describe('YearDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new YearDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
