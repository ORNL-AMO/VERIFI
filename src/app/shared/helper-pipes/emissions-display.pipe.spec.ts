import { EmissionsDisplayPipe } from './emissions-display.pipe';

describe('EmissionsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EmissionsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
