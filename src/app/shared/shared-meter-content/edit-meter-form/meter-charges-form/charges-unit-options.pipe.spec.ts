import { ChargesUnitOptionsPipe } from './charges-unit-options.pipe';

describe('ChargesUnitOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChargesUnitOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
