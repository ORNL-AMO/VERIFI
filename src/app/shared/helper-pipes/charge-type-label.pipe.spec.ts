import { ChargeTypeLabelPipe } from './charge-type-label.pipe';

describe('ChargeTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ChargeTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
