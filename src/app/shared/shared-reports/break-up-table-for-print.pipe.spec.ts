import { BreakUpTableForPrintPipe } from './break-up-table-for-print.pipe';

describe('BreakUpTableForPrintPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakUpTableForPrintPipe();
    expect(pipe).toBeTruthy();
  });
});
