import { InvalidMeterDataPipe } from './invalid-meter-data.pipe';

describe('InvalidMeterDataPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidMeterDataPipe();
    expect(pipe).toBeTruthy();
  });
});
