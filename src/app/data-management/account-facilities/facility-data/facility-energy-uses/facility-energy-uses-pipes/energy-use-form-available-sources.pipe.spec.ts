import { EnergyUseFormAvailableSourcesPipe } from './energy-use-form-available-sources.pipe';

describe('EnergyUseFormAvailableSourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyUseFormAvailableSourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
