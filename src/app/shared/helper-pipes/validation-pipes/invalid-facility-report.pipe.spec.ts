import { InvalidFacilityReportPipe } from './invalid-facility-report.pipe';

describe('InvalidFacilityReportPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidFacilityReportPipe();
    expect(pipe).toBeTruthy();
  });
});
