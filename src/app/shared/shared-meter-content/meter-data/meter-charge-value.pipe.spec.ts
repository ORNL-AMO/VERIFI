import { MeterChargeValuePipe } from './meter-charge-value.pipe';

describe('MeterChargeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MeterChargeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
