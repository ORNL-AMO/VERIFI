import { DisplayMeterDataDatePipe } from './display-meter-data-date.pipe';

describe('DisplayMeterDataDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMeterDataDatePipe();
    expect(pipe).toBeTruthy();
  });
});
