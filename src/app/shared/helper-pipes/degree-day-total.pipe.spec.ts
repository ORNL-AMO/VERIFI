import { DegreeDayTotalPipe } from './degree-day-total.pipe';

describe('DegreeDayTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeDayTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
