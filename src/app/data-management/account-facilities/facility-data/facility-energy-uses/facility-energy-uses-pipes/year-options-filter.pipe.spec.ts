import { YearOptionsFilterPipe } from './year-options-filter.pipe';

describe('YearOptionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new YearOptionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
