import { AnalysisCategoryPipe } from './analysis-category.pipe';

describe('AnalysisCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new AnalysisCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
