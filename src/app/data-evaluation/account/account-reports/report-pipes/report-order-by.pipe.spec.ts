import { ReportOrderByPipe } from './report-order-by.pipe';

describe('ReportOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
