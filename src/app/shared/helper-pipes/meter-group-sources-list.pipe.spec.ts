import { MeterGroupSourcesListPipe } from './meter-group-sources-list.pipe';

describe('MeterGroupSourcesListPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterGroupSourcesListPipe();
    expect(pipe).toBeTruthy();
  });
});
