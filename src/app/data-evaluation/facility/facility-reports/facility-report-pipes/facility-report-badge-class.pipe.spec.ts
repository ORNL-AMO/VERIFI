import { FacilityReportBadgeClassPipe } from './facility-report-badge-class.pipe';

describe('FacilityReportBadgeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityReportBadgeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
