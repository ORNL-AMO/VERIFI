import { ScopeLabelPipe } from './scope-label.pipe';

describe('ScopeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ScopeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
