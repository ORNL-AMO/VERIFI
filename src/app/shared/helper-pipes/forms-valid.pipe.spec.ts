import { FormsValidPipe } from './forms-valid.pipe';

describe('FormsValidPipe', () => {
  it('create an instance', () => {
    const pipe = new FormsValidPipe();
    expect(pipe).toBeTruthy();
  });
});
