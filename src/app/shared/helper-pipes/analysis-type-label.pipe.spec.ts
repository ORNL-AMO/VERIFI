import { AnalysisTypeLabelPipe } from './analysis-type-label.pipe';

describe('AnalysisTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AnalysisTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
