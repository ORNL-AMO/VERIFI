import { MeterSourceColorPipe } from './meter-source-color.pipe';

describe('MeterSourceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterSourceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
