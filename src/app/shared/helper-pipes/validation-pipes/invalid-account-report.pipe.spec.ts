import { InvalidAccountReportPipe } from './invalid-account-report.pipe';

describe('InvalidAccountReportPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidAccountReportPipe();
    expect(pipe).toBeTruthy();
  });
});
