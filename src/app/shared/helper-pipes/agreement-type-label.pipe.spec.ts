import { AgreementTypeLabelPipe } from './agreement-type-label.pipe';

describe('AgreementTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AgreementTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
