import { MeterChargePipe } from './meter-charge.pipe';

describe('MeterChargePipe', () => {
  it('create an instance', () => {
    const pipe = new MeterChargePipe();
    expect(pipe).toBeTruthy();
  });
});
