import { InvalidMeterPipe } from './invalid-meter.pipe';

describe('InvalidMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
