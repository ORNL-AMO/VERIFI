import { MeterGroupSourceConflictPipe } from './meter-group-source-conflict.pipe';

describe('MeterGroupSourceConflictPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterGroupSourceConflictPipe();
    expect(pipe).toBeTruthy();
  });
});
