import { EquipmentSourceIconsPipe } from './equipment-source-icons.pipe';

describe('EquipmentSourceIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentSourceIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
