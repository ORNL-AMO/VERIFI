import { OrderMeterDataByPipe } from './order-meter-data-by.pipe';

describe('OrderMeterDataByPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderMeterDataByPipe();
    expect(pipe).toBeTruthy();
  });
});
