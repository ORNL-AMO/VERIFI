import { TableFillPipe } from './table-fill.pipe';

describe('TableFillPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFillPipe();
    expect(pipe).toBeTruthy();
  });
});
