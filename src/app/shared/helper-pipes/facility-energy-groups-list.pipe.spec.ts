import { FacilityEnergyGroupsListPipe } from './facility-energy-groups-list.pipe';

describe('FacilityEnergyGroupsListPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityEnergyGroupsListPipe();
    expect(pipe).toBeTruthy();
  });
});
