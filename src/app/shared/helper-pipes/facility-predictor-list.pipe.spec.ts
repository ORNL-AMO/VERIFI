import { FacilityPredictorListPipe } from './facility-predictor-list.pipe';

describe('FacilityPredictorListPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityPredictorListPipe();
    expect(pipe).toBeTruthy();
  });
});
