import { SidebarItemActivePipe } from './sidebar-item-active.pipe';

describe('SidebarItemActivePipe', () => {
  it('create an instance', () => {
    const pipe = new SidebarItemActivePipe();
    expect(pipe).toBeTruthy();
  });
});
