import { YearOptionsPipe } from './year-options.pipe';

describe('YearOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new YearOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
